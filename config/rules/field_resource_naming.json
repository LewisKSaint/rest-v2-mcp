{
  "name": "field_resource_naming",
  "description": "Validates that field and resource naming follows Solace REST API conventions as per ADR",
  "enabled": true,
  "conditions": [
    {
      "type": "path_pattern",
      "pattern": "^/api/v2/[a-zA-Z]+/",
      "message": "URL structure must start with /api/v2/<product area>/"
    },
    {
      "type": "path_pattern",
      "pattern": ".*",
      "message": "Resources that are collections must be represented by plural nouns (ex. /api/v2/platform/environments)"
    },
    {
      "type": "path_pattern",
      "pattern": ".*",
      "message": "IDs that refer to other resources must be prefixed with the type of object (ex. organizationId)"
    },
    {
      "type": "path_pattern",
      "pattern": ".*",
      "message": "The resource being accessed should only use 'id' as the field"
    },
    {
      "type": "path_pattern",
      "pattern": ".*",
      "message": "The path must not include an organization ID variable as it is defined by the bearer token"
    },
    {
      "type": "resource_naming",
      "pattern": "^[a-z][a-zA-Z0-9]*$",
      "message": "Resources and fields must use camel case (ex. organizationName)"
    },
    {
      "type": "path_pattern",
      "pattern": ".*",
      "message": "No shortened words; prefer 'organization' over 'org' (with documented exceptions: admin, config, vpn, vpc, ec2)"
    },
    {
      "type": "path_pattern",
      "pattern": ".*",
      "message": "Camel case to be used for abbreviations (ex. awsVpc)"
    }
  ]
}
